# Changelog

TechPie 的所有重要变更。每个版本一节，按产品版本号排列（最新在最上面）。

<!--
约定（发布流程依赖这几条，改之前先看 CLAUDE.md → Releasing）：

- 一节对应一个**产品版本**（`## [1.0.0]`），不是某个候选版。同一条线的候选版和
  最终正式版共用这一节 —— 候选序号是发布时才派生的，没法提前写进标题。
- 发布时会**精确查找**这一节：没有、或者只有空标题，发布会直接被拒绝。
  所以准备发版时，先把这一节写好。
- 不要用 "Unreleased" 分节：发布查找的是版本号，写在那里等于没写。
- 这一节会被复制进 git tag 注解，GitHub Release 的说明就是从 tag 注解来的。
- 日期不必写在这里（tag 和 Release 自带时间）。
-->

## [1.0.1]

### 修复

- 校园网页：打开页面前先续期会话，退出登录后不再残留登录状态
- 云同步：恢复云端备份前先确认，避免覆盖本地内容
- 学生请假改用移动端入口
- 宽屏（桌面 / 平板）下从子页面返回不再直接退出应用，而是回到上一页
- 课表：绑定 eGate、续期会话或云同步更新账号之后会自动重新拉取，不再停在空表
- 课表：数据尚未到手时显示加载指示器，加载失败时提示重试，不再一律显示"本周没有课程"
- 课表：一次登录只发起一轮请求（此前启动、绑定、续期会各自拉一次）
- 检查更新：手动检查失败时说明原因，并可直接跳转 GitHub 查看最新版本
- 扫码：从页面底部滑入、退出时滑下，不再是瞬间切黑
- 校园卡：顶栏磨砂延伸到状态栏，内容可滚到屏幕最顶部
- Android 扫码：修复预览卡顿、分辨率低与扫描区域错位
- eCard：绑定响应必须来自绑定服务本身，回 200 但内容不对的响应不再被当成绑定成功
- eCard：域名解析不再限定具体应用，绑定不再依赖小程序的应用包名

### 新增

- 桌面端安装包：Linux（tar.gz）与 Windows（zip），此前只有 Android
- Android 通用安装包：一个包含全部 ABI 的 APK（arm64 / arm32 / x86_64）
- 校园签到：扫码提交签到，可查看签到公告与历史
- 校园网页支持附件选择、脚本注入与桌面端桥接消息
- HarmonyOS：扫码可用，校园签到等场景不必再手输
- 会话保活：可查看各节点的续期情况与下次续期时间
- eCard：可用小程序里的 6 位绑定码自动获取 OPENID 并连接（Android / HarmonyOS / iOS 真机，首次需授权一次 VPN）
- HarmonyOS：长按桌面图标可一键打开付款码

### 变更

- 扫码：识别到码后画面停在识别到的那一帧，不再额外截帧与编码，冻结更快也不会先黑一下
- 校园网页改用统一的会话存储，替换原有的 WebView 内嵌实现
- 桌面端宽窗口侧栏暂时关闭
- eCard：绑定页改用直白说明；自检不再自动运行，移到开发者实验室并显示服务端返回的原文
- 设置页的版本号现在可以点击：它会去 GitHub 检查有没有更新的版本，有就显示新版本号与更新日志，确认后跳转到发布页面
- 检查更新：启动与回到前台时会静默检查一次，只有确实有新版才提示；手动点击时无论结果如何都会给出反馈

## [1.0.0]

### 修复

- 离开课程录播页面后音频仍在播放
- 校园网页登录状态共享，以及退出登录时的会话清理
- 登录失败时保留登录页面并显示错误信息

### 新增

- 设置中加入隐私政策和账号注销入口

### 变更

- 正式构建隐藏开发者选项，禁用调试日志和本地开发服务器开关

后端修复仍待部署，部分校园服务可能暂不可用；本版本用于提前安装和验证客户端修复。
