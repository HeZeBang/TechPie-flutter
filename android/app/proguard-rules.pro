# Flutter's Android embedding and plugins provide their own consumer rules.
# Keep this file intentionally narrow: broad `-keep` rules would silently turn
# off the R8 shrinking and obfuscation enabled for release builds.
